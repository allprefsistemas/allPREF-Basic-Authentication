{% extends "template/baseLogin.html.twig" %}

{% block title %} Login {% endblock %}

{% block head %}
{{ parent() }}
<link href="{{ app.url_public }}/css/login.min.css" rel="stylesheet" type="text/css">
{% endblock %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <form class="form-signin" action="{{ app.url_base }}/login" method="POST">

                    <div class="logo">
                        <img src="{{app.url_public}}/imgs/allpref.jpg">
                    </div>

                    {% if app.my_session.has('success') %}
                        <div class="alert alert-success" role="alert">
                            {{ app.my_session.flash('success') }}
                        </div>
                    {% endif %}

                    {% if app.my_session.has('error') %}
                        <div class="alert alert-danger">
                            {{ app.my_session.flash('error') }}
                        </div>
                    {% endif %}

                    <div class="form-group">
                        <input type="email" name="email" class="form-control" placeholder="Email" required autofocus>
                    </div>

                    <div class="form-group">
                        <input type="password" name="senha" id="inputPassword" class="form-control"
                               placeholder="Password" required>
                    </div>

                    <div class="form-group">
                        <input type="hidden" name="_token" value="{{ token }}">
                        <button class="btn btn-primary btn-block" type="submit">Login</button>
                    </div>

                    <div class="form-group text-center">
                        <a href="{{ app.url_base }}/recover">Forgot Password?</a>
                    </div>

                    <div class="form-group text-center">
                        <a href="{{ app.url_base }}/signup">New user? Sign up</a>
                    </div>

                </form>
            </div>
        </div>
    </div>

{% endblock %}
