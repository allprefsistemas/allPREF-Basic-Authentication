<nav class="navbar navbar-inverse navbar-fixed-top shadow">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Navegação</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ app.url_auth }}/dashboard">
                allPREF
            </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">

            <ul class="nav navbar-nav navbar-right">
                <li {{ app.html.urlActive('config') }} class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">
                        {% set profileAvatar = (currentUser.avatar is not null)
                        ? app.url_public~'/imgs/avatar/'~currentUser.avatar
                        : app.url_public~'/imgs/user.png' %}
                        <img src="{{ profileAvatar }}" alt="User Avatar" class="user-avatar"> <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ app.url_auth }}/config/profile/"><i class="fa fa-user"></i> Profile</a>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <a href="{{ app.url_auth }}/logout"><i class="fa fa-sign-out"></i> Logout
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>